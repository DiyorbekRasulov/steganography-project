#include <stdio.h>
#include <stdlib.h>
#include "decode.h"

/*
 * decode.c
 *
 * Reads hidden bits back out of the image.
 *
 * We reconstruct bytes one bit at a time.
 */

uint8_t *decode_data(Image *image, int *output_size)
{
    /*
     * TODO:
     * - read LSBs
     * - rebuild bytes
     * - allocate output buffer
     * - return decoded data
     */

    return NULL;
}
