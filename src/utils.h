#ifndef UTILS_H
#define UTILS_H

/*
 * utils.h
 *
 * Helper functions shared across the project.
 *
 * Keep random helper logic here instead of
 * cluttering core files.
 */

void print_binary(unsigned char byte);

#endif
